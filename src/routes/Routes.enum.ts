/* eslint no-unused-vars: 0 */
export enum Routes {
    HOME = "/",
    ABOUT = "/about",
    CONTACT = "/contact",
    LOGIN = "/login",
    REGISTER = "/register",
    PROFILE = "/profile",
    PROJECTS = "/projects",
    CONFIRM_ACCOUNT = "/confirm-account/:token",
    FORGOT_PASSWORD = "/forgot-password",
}
